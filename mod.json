{
	"geode": "5.7.1",
	"gd": {
		"win": "2.2081",
		"android": "2.2081",
		"mac": "2.2081",
		"ios": "2.2081"
	},
	"id": "bluebeaker.level_song_offset",
	"name": "Level Song Offset",
	"version": "v1.0.0",
	"developer": "BlueBeaker",
	"description": "",
	"settings": {
		"negative-offset-fix": {
			"name": "Negative Offset Workaround",
			"description": "Automatically use workaround for the bug with negative offset",
			"type": "bool",
			"default": true
		},
		"padded-cache-path": {
			"name": "Padded Cache Path",
			"description": "Custom directory for storing padded audio cache files. Leave empty to use the mod's save directory",
			"type": "string",
			"default": "",
			"placeholder": "e.g. C:/Cache/LevelSongOffset"
		},
		"padded-cache-max-size": {
			"name": "Max Cache Size (MB)",
			"description": "Maximum disk space to use for padded audio cache. Oldest files are deleted when exceeded. 0 = unlimited",
			"type": "int",
			"default": 500,
			"min": 0,
			"max": 102400
		},
		"debug-logging": {
			"name": "Debug Logging",
			"description": "Enable detailed offset-related logging for debugging",
			"type": "bool",
			"default": false
		}
	},
	"resources": {
		"sprites": [
			"resources/offset-icon.png"
		]
	},
	"dependencies": {
		"geode.node-ids": ">=v1.23.3"
	}
}
