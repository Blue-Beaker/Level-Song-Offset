{
	"geode": "5.7.1",
	"gd": {
		"win": "2.2081",
		"android": "2.2081",
		"mac": "2.2081",
		"ios": "2.2081"
	},
	"id": "bluebeaker.level_song_offset",
	"name": "Level Song Offset",
	"version": "v1.0.0",
	"developer": "BlueBeaker",
	"description": "",
	"settings": {
		"negative-offset-fix": {
			"name": "Negative Offset Workaround",
			"description": "Automatically use workaround for the bug with negative offset",
			"type": "bool",
			"default": true
		},
		"padded-cache-path": {
			"name": "Negative Offset Audio Cache",
			"description": "Custom directory for storing padded audio cache files for the negative offset workaround. Leave empty to use the mod's save directory",
			"type": "string",
			"default": ""
		},
		"padded-cache-max-size": {
			"name": "Max Audio Cache Size (MB)",
			"description": "Maximum disk space to use for the negative offset audio cache. Oldest files are deleted when exceeded. 0 = unlimited",
			"type": "int",
			"default": 500,
			"min": 0,
			"max": 102400
		},
		"debug-logging": {
			"name": "Debug Logging",
			"description": "Enable detailed offset-related logging for debugging",
			"type": "bool",
			"default": false
		},
		"debug-beep-in-padding": {
			"name": "Debug: Beep in Padding",
			"description": "Add periodic beep tones in the silent padding to verify the padded audio is being used. Each beep = 100ms, every 500ms",
			"type": "bool",
			"default": false
		}
	},
	"resources": {
		"sprites": [
			"resources/offset-icon.png"
		]
	},
	"dependencies": {
		"geode.node-ids": ">=v1.23.3",
		"cvolton.level-id-api": ">=v2.0.0"
	}
}
